# 供 Docker Compose、命令行等使用（KEY=VALUE，无引号）
# 复制为项目根目录 .env 后按需修改
# 若用 IDE 直接跑 Spring Boot，更推荐使用 config/env.properties

PM_DB_URL=jdbc:mysql://mysql:3306/pm_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
PM_DB_USERNAME=root
PM_DB_PASSWORD=root
